<h1>タスク一覧</h1>
<p><%= notice %></p>
<table>
    <tr>
        <th>タスク内容</th>
    </tr>
    <tr>
    <% @tasks.each do |task| %>
        <td><%= task.details %></td>
    <% end %>
    </tr>
</table>

<%= link_to '新規投稿', new_task_path %>