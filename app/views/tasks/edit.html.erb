<h2>編集画面</h2>
<%= form_with(model: @task, local: true) do |form| %>
    <%= form.label :details %>
    <%= form.text_field :details %>   
    <%= form.submit %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>